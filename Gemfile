source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in setka_integration.gemspec
gemspec

group :development, :test do
  gem 'vcr'
  gem 'webmock'
  gem 'pry-byebug'
  gem 'bundler-audit'
end
